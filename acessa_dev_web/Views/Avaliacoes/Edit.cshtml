@model acessa_dev_web.Models.Avaliacao
@{
    ViewData["Title"] = "Editar Avaliação";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section Styles {
    <link rel="stylesheet" href="~/css/avaliacoes.css" />
}

<div class="container">
    <div class="page-header">
        <h1 class="page-title">Editar Avaliação</h1>
        <p class="page-subtitle">Atualize os dados da avaliação</p>
    </div>

    <div class="crud-container">
        <form asp-action="Edit" class="modal-form">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="idAvaliacao" />

            <div class="form-group">
                <label asp-for="DescricaoAvaliacao" class="form-label">Descrição *</label>
                <textarea asp-for="DescricaoAvaliacao" class="form-input" rows="4" required maxlength="500" placeholder="Descreva detalhes sobre a acessibilidade do local..."></textarea>
                <span asp-validation-for="DescricaoAvaliacao" class="validation-error"></span>
            </div>

            <div class="form-row">
                <div class="form-group">
                    <label asp-for="ValorAvaliacao" class="form-label">Nota *</label>
                    <div class="rating-stars" id="ratingStars">
                        <span class="star" data-value="1">★</span>
                        <span class="star" data-value="2">★</span>
                        <span class="star" data-value="3">★</span>
                        <span class="star" data-value="4">★</span>
                        <span class="star" data-value="5">★</span>
                    </div>
                    <input type="hidden" asp-for="ValorAvaliacao" id="valorAvaliacao" required>
                    <span asp-validation-for="ValorAvaliacao" class="validation-error"></span>
                    <small class="form-hint">Clique nas estrelas para dar a nota</small>
                </div>
                <div class="form-group">
                    <label asp-for="Data" class="form-label">Data *</label>
                    <input type="datetime-local" asp-for="Data" class="form-input" required>
                    <span asp-validation-for="Data" class="validation-error"></span>
                </div>
            </div>

            <div class="form-row">
                <div class="form-group">
                    <label asp-for="idLocal" class="form-label">Local *</label>
                    <select asp-for="idLocal" asp-items="ViewBag.idLocal" class="form-input" required>
                        <option value="">Selecione um local...</option>
                    </select>
                    <span asp-validation-for="idLocal" class="validation-error"></span>
                </div>
                <div class="form-group">
                    <label asp-for="idUsuario" class="form-label">Usuário *</label>
                    <select asp-for="idUsuario" asp-items="ViewBag.idUsuario" class="form-input" required>
                        <option value="">Selecione um usuário...</option>
                    </select>
                    <span asp-validation-for="idUsuario" class="validation-error"></span>
                </div>
            </div>

            <div class="modal-actions">
                <a href="@Url.Action("Index")" class="btn btn-secondary">Cancelar</a>
                <button type="submit" class="btn btn-primary">Salvar Alterações</button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <script src="~/js/avaliacoes.js"></script>
}